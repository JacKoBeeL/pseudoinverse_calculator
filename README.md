# pseudoinverse_calculator
A Python tool that uses custom-made classes with Householder reflections to calculate the Moore-Penrose Inverse (pseudoinverse) of a matrix. This tool works for real and complex matrices and does not use any libraries besides "deepcopy" and the standard Python math library.
